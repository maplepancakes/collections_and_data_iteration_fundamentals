namespace collections_and_data_iteration_fundamentals;

public class BasicLinqPractice
{
    /*
     *  Basic LINQ Methods
        Filtering -> .Where(...)
        Transforming -> .Select(...)
        Sorting -> .OrderBy(...), .OrderByDescending(...)
        First element -> .FirstOrDefault(...)
        Conversion -> .ToList()
        Existence check -> .Any(...), .All(...)
        Aggregation -> .Count(), .Sum()
     */
}